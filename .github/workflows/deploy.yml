name: Deploy Sentiel

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: sentiel-production
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          components: rustfmt, clippy
      - run: cargo fmt --all -- --check
      - run: cargo check --workspace
      - run: cargo test --workspace
      - run: cargo clippy --workspace --all-targets -- -D warnings
      - name: Scan tracked files for credentials
        shell: bash
        run: |
          if git grep -n -I -E 'BEGIN (RSA|OPENSSH|EC) PRIVATE KEY' -- ':!.github/workflows/deploy.yml' ':!README.md'; then
            exit 1
          fi

  security:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
      - name: Install security tooling
        run: |
          cargo install cargo-audit --locked
          cargo install cargo-deny --locked
      - name: Cargo audit
        run: cargo audit
      - name: Cargo deny
        run: cargo deny check advisories bans licenses sources
      - name: Gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Trivy filesystem scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: fs
          scan-ref: .
          scanners: vuln,secret,misconfig
          severity: HIGH,CRITICAL
          exit-code: 1

  deploy:
    needs: [verify, security]
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 22
          timeout: 120s
          command_timeout: 30m
          script: |
            set -euo pipefail

            if [ ! -d /opt/sentiel/.git ]; then
              rm -rf /opt/sentiel
              git clone https://github.com/rShetty/sentiel.git /opt/sentiel
            fi

            cd /opt/sentiel
            git fetch origin main
            git reset --hard origin/main
            git clean -fd

            # Ensure config and env directories
            mkdir -p /etc/sentiel
            if [ ! -f /etc/sentiel/sentiel.env ]; then
              echo "RUST_LOG=info" > /etc/sentiel/sentiel.env
            fi
            chmod 600 /etc/sentiel/sentiel.env

            # Copy config
            cp config/sentiel.toml /etc/sentiel/sentiel.toml

            # Build
            source "$HOME/.cargo/env"
            cargo build --release

            # Install binary
            install -m 755 target/release/sentiel /usr/local/bin/sentiel

            # Ensure user and data directory
            id sentiel >/dev/null 2>&1 || useradd --system --home-dir /var/lib/sentiel --create-home --shell /usr/sbin/nologin sentiel
            mkdir -p /var/lib/sentiel
            chown sentiel:sentiel /var/lib/sentiel

            # Install systemd unit
            cp deploy/sentiel.service /etc/systemd/system/sentiel.service
            systemctl daemon-reload
            systemctl enable sentiel
            systemctl restart sentiel

            # Health check with retries
            sleep 5
            for i in 1 2 3 4 5; do
              if curl -sf http://127.0.0.1:8585/health; then
                echo ""
                echo "DEPLOY SUCCESS: sentiel is healthy"
                exit 0
              fi
              echo "Health check attempt $i failed, waiting..."
              sleep 5
            done

            echo "ERROR: Health check failed"
            journalctl -u sentiel -n 30 --no-pager
            exit 1

      - name: Retry deploy (1)
        if: failure()
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 22
          timeout: 120s
          command_timeout: 30m
          script: |
            set -euo pipefail
            source "$HOME/.cargo/env"
            cd /opt/sentiel
            git fetch origin main
            git reset --hard origin/main
            cargo build --release
            install -m 755 target/release/sentiel /usr/local/bin/sentiel
            cp config/sentiel.toml /etc/sentiel/sentiel.toml
            cp deploy/sentiel.service /etc/systemd/system/sentiel.service
            systemctl daemon-reload
            systemctl restart sentiel
            sleep 5
            curl -sf http://127.0.0.1:8585/health && echo "DEPLOY SUCCESS (retry 1)" || (journalctl -u sentiel -n 20 --no-pager && exit 1)

      - name: Retry deploy (2)
        if: failure()
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 22
          timeout: 120s
          command_timeout: 30m
          script: |
            set -euo pipefail
            source "$HOME/.cargo/env"
            cd /opt/sentiel
            systemctl restart sentiel
            sleep 5
            curl -sf http://127.0.0.1:8585/health && echo "DEPLOY SUCCESS (retry 2)" || (journalctl -u sentiel -n 20 --no-pager && exit 1)
